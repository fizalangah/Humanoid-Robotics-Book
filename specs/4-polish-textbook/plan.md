# Implementation Plan: Polish Textbook: Front-Matter, Images, Formatting, Structure

**Branch**: `4-polish-textbook` | **Date**: 2025-12-06 | **Spec**: specs/4-polish-textbook/spec.md
**Input**: Feature specification from `/specs/4-polish-textbook/spec.md`

**Note**: This template is filled in by the `/sp.plan` command. See `.specify/templates/commands/plan.md` for the execution workflow.

## Summary

This feature aims to deliver a fully polished, error-free, and properly rendered Docusaurus textbook. This includes ensuring all chapters open correctly, eliminating front-matter leaks into content, replacing all image placeholders with real, AI-generated images, removing all default Docusaurus template files, and fixing general MDX formatting issues. The goal is to achieve a clean, valid, and consistently structured textbook that builds without errors and meets all quality standards.

## Technical Context

**Language/Version**: Markdown/MDX, TypeScript (for Docusaurus configuration and React components), Python (for potential image generation scripting or content parsing)
**Primary Dependencies**: Docusaurus, Node.js, React, potentially a Python environment for image generation
**Storage**: Filesystem (MDX files, images in `/static/img/auto/`)
**Testing**: `npm run build` (for Docusaurus compilation and link validation), manual visual inspection
**Target Platform**: Web (Docusaurus static site)
**Project Type**: Web application (static site generator)
**Performance Goals**: N/A (focus on content integrity and rendering correctness)
**Constraints**: Maintain valid MDX formatting and Docusaurus compatibility; strictly adhere to existing chapter content (except for front-matter and placeholders); generate context-relevant images; ensure all default Docusaurus content is removed; **if AI image generation fails, retry multiple times before marking as a failure**; no external dependencies beyond those implied by Docusaurus and image generation.
**Scale/Scope**: Impacts all existing MDX chapter files, Docusaurus configuration, and the `/static/img/` directory for new images. Requires scanning and modifying multiple files for front-matter, placeholders, and formatting. Potentially involves deleting directories and files (e.g., `docs/intro.md`, `tutorial-basics/`).

## Constitution Check

*GATE: Must pass before Phase 0 research. Re-check after Phase 1 design.*

The plan adheres to the project Constitution:
- **AI-Native Production:** All content fixes, image generation, and structural changes will be handled through SpecKit + Claude Code workflows.
- **Technical Accuracy:** Ensures correct MDX parsing, Docusaurus rendering, and valid image paths/components.
- **Beginner-Friendly, Consistency, Error-Free:** Directly addresses error-free builds, consistent formatting, and clean chapter structures.
- **Markdown Standards:** Explicitly targets correct front-matter placement and syntax, proper code blocks, and no stray characters.
- **Docusaurus Standards:** Ensures MDX compiles without errors/warnings, valid sidebar categories (implicitly by removing boilerplate), and correct image storage paths (`/static/img/auto/`).
- **AI Implementation Rules:** Adheres to SpecKit workflows and avoids breaking Docusaurus structure.
- **Success Criteria:** Aligns perfectly with the goal of a fully polished, error-free, and properly rendered Docusaurus book.

No violations are detected as this comprehensive task focuses on improving the quality and integrity of the existing textbook project in alignment with all constitutional rules.

## Project Structure

### Documentation (this feature)

```text
specs/4-polish-textbook/
├── plan.md              # This file (/sp.plan command output)
├── research.md          # Phase 0 output (if needed, currently N/A)
├── data-model.md        # Phase 1 output (if needed, currently N/A)
├── quickstart.md        # Phase 1 output (if needed, currently N/A)
├── contracts/           # Phase 1 output (if needed, currently N/A)
└── tasks.md             # Phase 2 output (/sp.tasks command - NOT created by /sp.plan)
```

### Source Code (repository root)

```text
docs/
├── **/*.mdx             # All MDX chapters to be scanned and potentially modified

static/
└── img/
    └── auto/            # Directory for AI-generated images

src/
└── pages/
    └── index.tsx        # May need inspection for default Docusaurus links

docusaurus.config.ts     # May need inspection for default Docusaurus configurations
sidebars.ts              # May need inspection for default Docusaurus configurations

# Files/directories to be deleted:
intro.md                 # Example default chapter
tutorial-basics/         # Example default tutorial directory
tutorial-extras/         # Example default tutorial directory
# Potentially other autogenerated examples
```

**Structure Decision**: The project structure already largely supports these changes. The key additions will be new image files within `static/img/auto/` and the removal of specific default Docusaurus files and directories. Modifications will be concentrated in existing MDX files and potentially `docusaurus.config.ts` or `sidebars.ts` if default references are found.

## Complexity Tracking

> **Fill ONLY if Constitution Check has violations that must be justified**

N/A - No constitution violations have been identified.
